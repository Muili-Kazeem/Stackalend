import { PreviewTagPipe } from './preview-tag.pipe';

describe('PreviewTagPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewTagPipe();
    expect(pipe).toBeTruthy();
  });
});
